import React from 'react';
import { render, screen } from '@testing-library/react';
import Nav from '../components/Nav';

describe('show nav bar correctly', () => {
  it('renders without logged in user', () => {
    
  });

  it('renders with logged in user', () => {
    
  });
})